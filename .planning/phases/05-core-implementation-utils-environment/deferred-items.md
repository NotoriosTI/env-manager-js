# Deferred Items

- Plan `05-01`: `tests/bool-to-string-coercion.test.ts` is still blocked by `src/manager.ts` throwing `Not implemented` in `ConfigManager`, so the cross-layer bool-to-string verification remains deferred to the manager implementation phase.
